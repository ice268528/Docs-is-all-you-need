---
文档定位: "当前阶段目标与边界。"
作者: "Planning AI 起草，Owner 定稿"
目标对象:
  - "Owner"
  - "Code Agent"
Agent权限: "询问后可改"
当前操作者提醒:
  - "本文件只定义当前阶段范围、交付项、验收标准与明确不做的内容。"
  - "不要在此重复项目背景；项目级信息请引用 /docs/project/project_brief.md。"
  - "新增需求只有在 Owner 明确决定并入当前阶段后，才应写入本文件。"
---

<!-- 本模板建议另存为 /docs/stages/stage_XX_goal.md -->
# stage_XX_goal

## 1. 阶段信息

- 阶段编号：`<此处填写 stage_XX>`
- 阶段名称：`<此处填写>`
- 当前状态：`<此处填写 draft / confirmed / in_progress / done>`
- 阶段一句话目标：`<此处填写>`
- 关联项目说明：`/docs/project/project_brief.md`
- 关联实现约束：`/docs/project/implementation_constraints.md`
- 关联 TODO：`/TODO.md`
- 相关 REQ：`<此处填写已并入或待并入的 REQ；无则写 无>`

## 2. 完成定义（Definition of Done）

- [ ] `<此处填写本阶段完成后必须成立的总体结果>`
- [ ] `<此处填写本阶段完成后必须成立的总体结果>`

## 3. 前置依赖 / 进入条件

<!-- 只写影响本阶段启动或推进的前提条件，不写具体编码步骤。 -->

- [ ] `<此处填写依赖条件>`
- [ ] `<此处填写依赖条件>`

## 4. 本阶段交付项

### D-01 `<此处填写交付项名称>`

- 目标：`<此处填写交付项要解决什么>`
- 范围内：`<此处填写包含哪些行为/页面/接口/结果>`
- 明确不含：`<此处填写与该交付项容易混淆但本阶段不做的部分>`
- 前置依赖：`<此处填写 无 / REQ_xxx / 决策 / 外部条件>`
- 关联文档：`<此处填写 REQ / 报告 / 设计资料；无则写 无>`

#### 验收标准（Verify）

- [ ] `<此处填写功能/行为级验收标准>`
- [ ] `<此处填写测试或观察方式>`
- [ ] `<此处填写是否需要 Owner 手测以及关注点>`

### D-02 `<此处填写交付项名称>`

- 目标：`<此处填写>`
- 范围内：`<此处填写>`
- 明确不含：`<此处填写>`
- 前置依赖：`<此处填写>`
- 关联文档：`<此处填写>`

#### 验收标准（Verify）

- [ ] `<此处填写>`
- [ ] `<此处填写>`

## 5. 阶段范围外（明确不做）

<!-- 本节用于防止阶段边界漂移。 -->

- [ ] `<此处填写当前阶段不做的事项>`
- [ ] `<此处填写当前阶段不做的事项>`

## 6. 风险、假设与待 Owner 决策

- 主要风险：`<此处填写>`
- 关键假设：`<此处填写>`
- 待 Owner 决策：`<此处填写；无则写 无>`
- 若决策未定的默认策略：`<此处填写；若不能默认则写 不能默认，需先确认>`

## 7. 验收总表

| 交付项 | 验收点 | Verify 方式 | 责任人 |
|---|---|---|---|
| `D-01` | `<此处填写>` | `<自动测试 / 手动验证 / 构建 / 类型检查 / 其他>` | `<Code Agent / Owner>` |

## 8. 需求并入记录

| REQ 文档 | 是否并入当前阶段 | 决定日期 | 备注 |
|---|---|---|---|
| `<此处填写 /docs/changes/REQ_*.md>` | `<是 / 否 / 待定>` | `<此处填写>` | `<此处填写>` |

## 9. 阶段收尾应输出的产物

- [ ] `/docs/handoffs/<此处填写 stage_XX_summary.md>`
- [ ] `/docs/testing/<此处填写 stage_XX_manual_test.md>`
- [ ] `/docs/TODO_backup/<此处填写 TODO_YYYYMMDD_HH.md>`
