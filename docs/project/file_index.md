---
文档定位: "项目文件索引。"
作者: "Code Agent 初稿"
目标对象:
  - "Code Agent"
  - "Planning AI"
  - "Owner"
Agent权限: "询问后可改"
当前操作者提醒:
  - "本文件列出重要目录、关键文件、入口文件、配置文件与测试文件位置。"
  - "这里只做索引与导航，不在此解释完整架构，也不承载任务状态。"
  - "若目录结构发生明显变化，更新前应先说明范围并取得 Owner 确认。"
---

<!-- 本模板建议另存为 /docs/project/file_index.md -->
# file_index

## 1. 索引范围与更新基准

- 索引范围：`<此处填写覆盖的仓库/目录范围>`
- 最近核对基线：`<此处填写分支 / 提交 / 日期>`
- 排除范围：`<此处填写不纳入索引的目录；无则写 无>`
- 维护说明：`<此处填写按何种粒度维护>`

## 2. 根目录关键项

| 路径 | 类型 | 作用 | 什么时候需要读 | 修改注意事项 |
|---|---|---|---|---|
| `<此处填写>` | `<目录 / 文件>` | `<此处填写>` | `<此处填写>` | `<此处填写>` |

## 3. 应用入口与路由文件

| 路径 | 类型 | 作用 | 备注 |
|---|---|---|---|
| `<此处填写>` | `<入口 / 路由 / 布局>` | `<此处填写>` | `<此处填写>` |

## 4. 关键业务模块

| 模块/目录 | 责任 | 上游依赖 | 下游影响 | 备注 |
|---|---|---|---|---|
| `<此处填写>` | `<此处填写>` | `<此处填写>` | `<此处填写>` | `<此处填写>` |

## 5. 配置与环境文件

| 路径 | 用途 | 环境影响 | 修改风险 |
|---|---|---|---|
| `<此处填写>` | `<此处填写>` | `<此处填写>` | `<此处填写>` |

## 6. 测试相关文件

| 路径 | 测试类型 | 覆盖内容 | 备注 |
|---|---|---|---|
| `<此处填写>` | `<unit / integration / e2e / manual>` | `<此处填写>` | `<此处填写>` |

## 7. 构建 / 部署 / 脚本入口

| 路径 | 用途 | 触发方式 | 备注 |
|---|---|---|---|
| `<此处填写>` | `<此处填写>` | `<此处填写>` | `<此处填写>` |

## 8. 高风险或慎改区域

- [ ] `<此处填写高风险路径 + 原因>`
- [ ] `<此处填写高风险路径 + 原因>`

## 9. 待补全索引项

- [ ] `<此处填写尚未补齐但后续必须补充的文件或目录>`
- [ ] `<此处填写尚未补齐但后续必须补充的文件或目录>`
